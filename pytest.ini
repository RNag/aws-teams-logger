[pytest]
log_cli = 1
log_cli_format = %(levelname)s %(message)s
log_cli_level = INFO
markers =
    mutative: mark a test as potentially dangerous one
    long: a longer integration test, such as a performance test